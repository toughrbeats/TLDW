name: pipeline_sadtalker
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - pytorch=2.0.1=*cpu*
  - pip
  - pip:
      - gradio
      - imageio
      - imageio-ffmpeg
      - opencv-python
      - numpy
      - ffmpeg-python
      - face_alignment
      - insightface
      - gfpgan
